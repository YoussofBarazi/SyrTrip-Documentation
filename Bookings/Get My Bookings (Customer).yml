info:
  name: Get My Bookings (Customer)
  type: http
  seq: 2

http:
  method: GET
  url: "{{Host}}/api/bookings/my-bookings"
  headers:
    - name: Authorization
      value: Bearer {{Token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
