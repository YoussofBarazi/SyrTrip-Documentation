info:
  name: Get Car Offices (Public)
  type: http
  seq: 1

http:
  method: GET
  url: "{{Host}}/api/offices"
  params:
    - name: search
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
