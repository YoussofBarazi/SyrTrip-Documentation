info:
  name: Get My Profile (All Roles)
  type: http
  seq: 1

http:
  method: GET
  url: "{{Host}}/api/users/me"
  headers:
    - name: Authorization
      value: Bearer {{Token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
