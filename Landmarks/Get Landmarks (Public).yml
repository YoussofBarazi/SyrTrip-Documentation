info:
  name: Get Landmarks (Public)
  type: http
  seq: 1

http:
  method: GET
  url: "{{Host}}/api/landmarks"
  params:
    - name: page
      value: "1"
      type: query
      disabled: true
    - name: limit
      value: "10"
      type: query
      disabled: true
    - name: search
      value: ""
      type: query
      description: Search for name or location
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
