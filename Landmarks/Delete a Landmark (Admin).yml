info:
  name: Delete a Landmark (Admin)
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{Host}}/api/landmarks/{{landmark_id}}"
  headers:
    - name: Authorization
      value: Bearer {{Token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
