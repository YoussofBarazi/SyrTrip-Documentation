info:
  name: Delete a Restaurant (Admin or Owner)
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{Host}}/api/restaurant/{{restaurant_id}}"
  headers:
    - name: Authorization
      value: Bearer {{Token}}
  auth: inherit

runtime:
  variables:
    - name: restaurant_id
      value: 3a257d86-e5cb-4117-99d4-4fe7de65b4fc

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
