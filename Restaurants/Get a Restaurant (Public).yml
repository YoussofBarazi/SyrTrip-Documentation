info:
  name: Get a Restaurant (Public)
  type: http
  seq: 3

http:
  method: GET
  url: "{{Host}}/api/restaurants/{{restaurant_id}}"
  auth: inherit

runtime:
  variables:
    - name: restaurant_id
      value: 3a257d86-e5cb-4117-99d4-4fe7de65b4fc

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
