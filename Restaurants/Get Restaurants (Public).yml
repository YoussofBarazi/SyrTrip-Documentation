info:
  name: Get Restaurants (Public)
  type: http
  seq: 2

http:
  method: GET
  url: "{{Host}}/api/restaurants"
  params:
    - name: page
      value: "1"
      type: query
      disabled: true
    - name: limit
      value: "10"
      type: query
      disabled: true
    - name: search
      value: ""
      type: query
      description: search for name or location
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
