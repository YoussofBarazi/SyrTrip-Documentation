info:
  name: Delete Car (Admin or Office Owner)
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{Host}}/api/cars/{{carId}}"
  headers:
    - name: Authorization
      value: Bearer {{Token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
