info:
  name: Send Admin Notification (Admin)
  type: http
  seq: 4

http:
  method: POST
  url: "{{Host}}/api/notifications/admin/send"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
