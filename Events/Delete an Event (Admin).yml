info:
  name: Delete an Event (Admin)
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{Host}}/api/events/{{event_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
