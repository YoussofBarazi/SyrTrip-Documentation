info:
  name: Get Single Event (Public)
  type: http
  seq: 2

http:
  method: GET
  url: "{{Host}}/api/events/{{event_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
